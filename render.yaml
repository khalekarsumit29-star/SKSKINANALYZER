services:
  - type: web
    name: dermify-ai
    runtime: node
    nodeVersion: "20"
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
